use crate::domain::bond::Bond;

/// Executes the `current-yield` command
pub fn run_current_yield(bond: Bond) {
    unimplemented!("Wire CLI to current_yield()")
}

/// Executes the `simple-yield` command
pub fn run_simple_yield(bond: Bond, years: f64) {
    unimplemented!("Wire CLI to simple_yield()")
}
